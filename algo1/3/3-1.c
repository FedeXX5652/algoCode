#include <stdio.h>

int main(){

    int nro;
    char letra;
    
    if(nro>10){
        int nro2;         // Todo lo que se cree dentro de un bloque, se destruye al terminar el mismo
    }
    else{
        char letra2;
    }

    return 0;

}